# 2026-08-23 第 17 輪（日常巡查）

執行者：Claude（Sonnet 5）｜人類在場：無（排程自動觸發）

---

## 0. 框架挑戰（GOVERNANCE §0.5）

例行工作（T1，內容產出）—— 依 `daily-survey.md` 步驟 0 但書可跳過完整 Q1–Q7，**跳過本身記錄於此**。

開工前依序讀 `ops/PENDING-REVIEW.md`、`ops/HARD-RULES.md`、`ops/CHARTER.md`（§0、§1、§3）、
`ops/BACKLOG.md`、`ops/METRICS.md`、`ops/NOT-DONE.md`、最近一篇 JOURNAL（2026-08-22）。

**環境筆記**：工作目錄同樣是 detached HEAD、落後遠端一個 commit（08-22 watchdog 更新）。
`git fetch origin main && git checkout -B main origin/main` 對齊——這是連續第 3 輪遇到同一
狀況（08-21、08-22 皆記過），下一輪應假設這是常態而非例外。

用 GitHub API（`list_pull_requests`）核對 PR 佇列：**7 件仍全部未合併，`updated_at` 與
`created_at` 完全相同（零互動）**。PR #1（2026-08-07 開）今天滿 **17 天**；#2（08-10）14
天；#3（08-12）12 天；#4（08-13）11 天；#5（08-14）10 天；#6（08-15）9 天；#7（08-16）8
天。狀態與 08-22 記錄一致。**本輪不重複發送這個事實的通知**——08-19～08-21 已連續三次
通知同一未變狀態，08-22 已判斷再通知是疲勞式提醒不是新資訊，今天狀態依然沒變，沿用
08-22 的判斷。

## 1. 這輪想回答什麼

對應 **A 軸（記載軸）**。B-003（五篇既有內容補完）已於 08-16 結案，但那五篇的研究成果
全部卡在待審 PR，`content/` 在 main 上實際仍是零篇有完整來源。08-22 的 B-020 辯證探究
（見 corpus-0005）已經找到兩篇具名、第一人稱、直接核對過全文的機構媒體報導，材料完整、
查證扎實，08-22 日誌的「下一步」明確寫著這批材料「具備 seed 一篇新 `content/` 的基礎」，
只是當時判斷「依現行 PR 佇列積壓判斷，本輪不新開內容 PR，留待佇列疏通後再議」。

**今天推翻這個判斷，理由記在此處**：PR 佇列積壓是人類端的審核產能問題，不是這批材料
本身的品質問題。不開新 PR 並不會讓既有 7 件 PR 更快被合併，只會讓一份已經查證扎實、
通過稽核零 FAIL 零 WARN 的內容，繼續留在 `ops/CORPUS/` 裡不對外可見。`daily-survey.md`
步驟 1.5 明寫「一天最多開一個 PR」是上限、不是「佇列有積壓就不開」的理由；`CHARTER.md`
§3 也明確授權迴圈可以自主開 T3 PR。判斷：把材料寫成內容，開第 8 件 PR，並在 PR 說明裡
如實寫明佇列現況，讓人類一次看到八件待審項目，而不是替人類決定「先不要」。

## 2. 執行

把 corpus-0005（08-22 辯證探究產出）的 s-0001、s-0002、s-0003 三筆機構媒體來源，寫成新
內容 `content/kavalan-zhangyuan.md`（台東噶瑪蘭族：樟原部落的當代聲音）。過程：

1. **重新核實授權狀態**：corpus-0005 原始記錄 `news.ipcf.org.tw` 兩篇來源的 `license`
   皆為 `unknown`（該欄位在 CORPUS 記錄裡本來就允許暫時 unknown，但 H14 明文規定
   `license: unknown` 者不得進入網站內容——這是今天寫作前第一件要解決的事，不能直接
   照搬 CORPUS 的來源清單）。本輪重新 WebFetch 兩篇報導頁尾，確認明確版權聲明：
   「財團法人原住民族文化事業基金會 版權所有 Copyright © 2021 Indigenous Peoples
   Cultural Foundation All Rights Reserved.」。依 B-023 既有先例（自由時報、公視新聞網、
   東部海岸國家風景區管理處皆採同一處理方式：明確的已知著作權狀態標為
   `all-rights-reserved`，不是 `unknown`，僅引用事實與具名引語，不重製全文），三筆來源
   （s-0001、s-0002、標籤頁 s-0003）皆標 `license: all-rights-reserved`，通過 H14。
2. **同時重新存取確認內容未變**：兩篇報導頁面今天（2026-08-23）重新 WebFetch，確認仍
   可讀取、內容與 08-22 記錄一致（陳天祿班級規模記憶、藤天祿／潘真蔚／陳文龍的海祭發言），
   `accessed` 更新為今天的日期，而非沿用 08-22 的存取記錄。
3. **正文撰寫**：三段內容，一段一來源，不融合。刻意保留並凸顯 corpus-0005 c-0007 已發現
   的姓氏矛盾（陳天祿／藤天祿）作為正文的一部分，而不是為了敘事流暢而挑一個版本——這是
   ENTITIES.md P5「不擅自合併」原則直接落實到網站內容，不只是留在 `ops/` 研究紀錄裡。
   最後一段刻意寫「誠實的侷限」：引用 c-0009 的量化觀察（TITV 標籤頁 12 篇報導僅 2 篇
   主體在台東，約 17%），並明白寫出樣本數小、不足以推論精確比例，避免給讀者「這篇文章
   已經填平缺口」的錯誤印象。
   海祭儀式進行的細節本身全文未描述，依 H13 精神與 corpus-0005 原始記錄的處理方式一致。
4. **frontmatter**：`indigenous: true`、`tk_notice: open-to-collaborate`（沿用 surfing.md／
   amis.md PR 版本的既有格式，非自創新值）、`spatial_level: settlement`、
   `as_of: 2026-08-23`、`ai_in_methods: ["來源蒐集", "連結查證"]`、
   `ai_in_acknowledgment: ["摘要"]`。座標（樟原）用長虹橋／八仙洞之間的概略位置估計，
   非精確測量，之後有更準確資料應更正。

## 3. 稽核

```
npx tsx scripts/audit-content.ts
```

`content/kavalan-zhangyuan.md`：**0 FAIL、0 WARN**——本輪新增的六篇裡最乾淨的一篇，
輸出裡完全沒有列出這個檔案的任何項目。既有五篇（含 main 上的舊版）仍是 46 筆既有債務，
本輪未變動。完整輸出：`檢查 6 篇內容 ｜ 0 新 FAIL ｜ 46 既有債務(baseline) ｜ 34 WARN`。
（`npm ci` 因 B-015 已知的 lockfile／Node 版本不同步問題失敗，改用 `npm install`，
`package-lock.json` 產生的 diff 未 commit，做法與前幾輪相同。）

## 4. 推翻了什麼

**沒有推翻任何內容性假設**——今天是把已查證的材料轉為內容，不是新的辯證探究。但推翻了
**08-22 自己做的一個流程判斷**：「佇列積壓 → 暫不新開內容 PR」。理由見上方第 1 節。
這個推翻值得記錄，因為它是迴圈對自己前一輪決定的重新檢視，而不是對外部命題的推翻——
連續多輪「不新開 PR」的判斷若不被檢視，會變成一種未經明說的政策，而這從未被人類授權。

## 5. 失敗與卡關

- 樟原精確座標查無公開數據（觀光署頁面只有文字描述，無經緯度），本輪用地標推估，已在
  §2.4 標註限制。
- 陳天祿／藤天祿姓氏矛盾（corpus-0005 c-0007）本輪未解決，正文如實保留矛盾。

## 6. PR 佇列狀態

7 件既有 PR 仍零互動（見 §0）。本輪新開第 8 件 PR（`content/kavalan-zhangyuan.md`），
會在 PR 說明裡註明佇列現況，讓人類一次看到完整待審清單，不做篩選。

## 7. 下一步

- 第 8 件 PR 待人類 review／merge。
- corpus-0005 c-0007（姓氏矛盾）與 c-0009（量化佐證樣本數過小）仍是未解決的線索，
  下一輪若有餘力可延伸查證，非高優先。
- B-027：延續 08-22 F66 的教訓，候選核實應優先從機構媒體 site 搜尋角度找，而非只依賴
  GRB/NDLTD 批次匯出候選清單。
- 建議下一輪重新評估「今天要不要開新內容 PR」時，預設值應該是「開」而不是「因為佇列
  積壓所以不開」——除非有具體理由（例如材料本身查證強度不足），佇列長度本身不是理由。

---

本輪 `ops/JOURNAL`、`ops/METRICS.md` 直接 commit 到 main（研究紀錄，非網站內容，依步驟
1.5 範圍表）。`content/kavalan-zhangyuan.md` 命中族群名／部落名／族語詞，`indigenous: true`，
依 H10 走 PR，非 main 直接 commit。
