# 2026-09-11 第 36 輪（排程觸發的日常巡查）

執行者：Claude（Sonnet 5）｜人類在場：無（排程自動觸發）

---

## 0. 框架挑戰（GOVERNANCE §0.5）

本輪執行的是 BACKLOG 裡一個已查證超過一個月、但從未被排進執行的既有項目（B-004），不是全新研究方向，依 `daily-survey.md` 步驟 0 但書可跳過完整 Q1–Q7，**跳過本身記錄於此**。

## 1. 這輪想回答什麼

開工先讀 `ops/PENDING-REVIEW.md`（16 件開著 PR，最舊 PR #2 已逾 32 天）、`ops/HARD-RULES.md`、`ops/BACKLOG.md`（完整讀）、`ops/CHARTER.md`、最近一篇 JOURNAL（09-10）、`ops/METRICS.md`。

**先核對排程指令本文與 repo 實況的落差**：本輪排程 prompt 沿用的仍是專案早期「METRICS 的『有完整來源的內容』停在 0/5」這個舊描述，與 09-09／09-10 兩輪已記錄過的落差同性質，本輪同樣依即時狀態判斷，不依舊數字重做工作。

依 `daily-survey.md` 步驟 2 判準逐項核對：

1. **還有 `status: seed` 且來源不完整的既有內容嗎？** 本輪跑 `npx tsx scripts/audit-content.ts`（`npm install` 而非 `npm ci`，見 §5）確認：main 上 8 篇內容中，`amis.md`／`surfing.md` 仍有 baseline FAIL，但補完工作**已分別完成於待審的 PR #7、PR #4**（B-003 已於 08-16 結案），不重做。
2. **B-007（七組高風險同名詞叢集）已於 09-10 全數結案**（PR #17-23），本輪不延續。
3. 用 GitHub API 逐一核對 PENDING-REVIEW 清單真實狀態（見 §2），確認 PR #3／#5／#6／#10／#11／#12 六件在 08-28 已由人類（akai1030 帳號，`merged_by` 欄位確認）合併，`list_pull_requests` 摘要欄位一度顯示這幾件 `merged:false`——用 `pull_request_read`（`get` method）逐一覆核後確認是列表端點欄位讀取的顯示問題，非真實合併狀態有誤，實際 `merged:true`、`merged_at: 2026-08-28T07:57:00Z`。此為工具行為的核對，非內容性推翻，不計入 §4。
4. 抽查 PR #2、#22 的既有留言／review，確認 09-03、09-09 兩輪記錄的「akai1030 帳號留言其實是 agent session 自己用人類帳號回覆 Codex bot」判斷仍然成立（留言簽名皆為「Generated by Claude Code」），**16 件待審 PR 至今仍是零真人互動**，與 09-10 記錄一致，非新發現。

**兩條路都動不了新內容**（B-003、B-007 皆已結案，成果卡在審核佇列），依步驟 2.3「兩者都動不了 → 從 BACKLOG 挑一件」，逐一檢視 🟠 高優先清單後選定 **B-004**（修正 `docs/CLAUDE.md` 噶瑪蘭族陳述）：這是 2026-08-06 第 1 輪就查證完成（`verified`）的項目，被明確標記「Tier T3 → 開 PR」，但一個多月來每一輪都優先做 B-007 的同名詞示範，**從未被排進執行**——依 `CHARTER.md` §5 每月回顧的提問「BACKLOG 裡有沒有一直被跳過的項目？為什麼？」，這正是一個具體案例，值得在月度回顧前先處理掉，而不是繼續累積。對應 **A 軸**（記載軸：修正本專案自身基礎文件裡的錯誤記載）。

## 2. 檢索紀錄（PRISMA-S）

| 平台 | 檢索式／操作 | 日期 | 語言 | 結果 | 納入 |
|---|---|---|---|---|---|
| GitHub API | `list_pull_requests`（state=all, sort=updated） | 2026-09-11 | — | 24 件 PR 全量列表，含 8 件 `state:closed` | 是 |
| GitHub API | `pull_request_read get`，PR #3 | 2026-09-11 | — | 確認 `merged:true`，`merged_by: akai1030`，`merged_at: 2026-08-28T07:57:00Z` | 是（核對列表端點欄位不一致） |
| GitHub API | `pull_request_read get_comments`，PR #2 | 2026-09-11 | — | 三則留言，兩則 akai1030 帳號但簽名為 Claude Code 生成，一則 Codex bot | 是 |
| GitHub API | `pull_request_read get_reviews` / `get_review_comments`，PR #22 | 2026-09-11 | — | 3 個 review thread，Codex 提出、akai1030 帳號當場回覆並修正，皆為 agent session 行為 | 是 |
| Bash | `npx tsx scripts/audit-content.ts`（本機） | 2026-09-11 | — | main 上 8 篇：0 新 FAIL／20 既有 baseline／18 WARN | 是 |
| WebSearch | `原住民族委員會 噶瑪蘭族 臺東縣 人口 2025 統計` | 2026-09-11 | zh | 命中原民會「114 年第 6 週」等統計入口頁，但分族群分縣市數字僅提供 Excel 批次檔，摘要層未給出臺東縣噶瑪蘭族具體現況數字 | 是（判斷不足以升級，未採用） |

## 3. 發現

### F133（工具行為核對，非內容發現）—— GitHub `list_pull_requests` 的 `merged` 欄位對已合併又關閉的 PR 顯示不可靠

`list_pull_requests`（`state: all`）回傳 PR #3／#5／#6／#10／#11／#12 皆為 `"state":"closed","merged":false`，但逐一用 `pull_request_read`（`get`）核對，六件全部是 `merged:true`，且 `merged_by` 明確是 `akai1030`（人類帳號）、`merged_at` 落在 2026-08-28。這與 `content/green-island.md` 等六篇本輪 `audit-content.ts` 顯示已零 FAIL、且 `ops/METRICS.md` 08-28 欄位早已記錄「人類首次合併 6 件非 T3 PR」的既有記錄一致——**不是新的合併事件**，只是本輪核對工具本身時，發現用來列舉 PR 的 API 端點在這個欄位上不可信，逐案要用 `get` 方法才拿得到正確值。下一輪若要快速核對 PR 真實合併狀態，應避免只看 `list_pull_requests` 摘要欄位。

### F134（`verified`，A 軸，延續 08-06 F1／08-08 F7 既有查證）—— B-004 執行：`docs/CLAUDE.md` 噶瑪蘭族陳述改寫完成

原文「噶瑪蘭族（Kavalan）— 長濱鄉，約 113 人，宜蘭南遷族群」同時犯 08-06 F1 指出的三種錯（時間混淆：113 人是 2002 年數字；層級混淆：113 是縣層級非鄉層級；實體過度簡化：長濱鄉底下實際是三間村、樟原村、大峰峰等具體聚落，且花蓮豐濱鄉新社／立德才是文化保存最完整、人口最集中處，臺東為次要分布地）。本輪未重新檢索——F1（`verified`，原民會／臺東縣原民處／臺灣原住民數位博物館交叉支持）與 08-08 F7（大峰峰／三間村聚落記載，`attested`）皆一個月來未被推翻，直接沿用既有查證結果改寫，並於文中註明來源出處（`ops/JOURNAL/2026-08-06.md` F1、`ops/JOURNAL/2026-08-08.md` F7）供後續稽核追溯。

### F135（`gap`，方法限制）—— 嘗試尋找噶瑪蘭族臺東縣現況（2025年）人口數字未果，非查過確認沒有

本輪額外嘗試：F134 改寫時只能用 2002 年的舊數字，若能找到內政部／原民會近年（如 2025 年）分縣市分族別的現況人口，本可讓 `docs/CLAUDE.md` 這行更完整。WebSearch 命中原民會「114 年第 6 週」等統計入口頁，但確認具體數字需要下載並解析官方 Excel 批次檔（依過往 GRB／NDLTD 經驗，這類批次檔案本身可解析，但本輪判斷這是一個獨立的資料工程小任務，超出「修正一句已知錯誤陳述」的範圍），本輪未下載執行，如實記為留待下一輪或另立新 BACKLOG 項目，不影響本輪已完成的修正（本輪修正的重點是移除錯誤的時間／層級混淆標示，不是提供最新數字）。

## 4. 推翻了什麼

**無**——本輪執行的是 08-06 F1 已查證完成、一個月來未被任何後續證據挑戰的既有結論，不是新推翻。F133 是工具行為的核對（GitHub API 欄位不可靠），不是本站對台東的研究性推翻，依既有計算口徑不計入本欄（與 09-07、09-08 兩輪「查核本站內部工具缺陷不計入」的先例一致）。

## 5. 失敗與卡關

- `npm ci` 在本次沙箱環境因 lockfile 與 `package.json` 不同步失敗（`Missing: @swc/helpers@0.5.23 from lock file`），改用 `npm install` 成功安裝，過程中 `package-lock.json` 被本地工具重寫，本輪未提交這個 lockfile diff（與 B-015 記錄的既有已知問題同構，非本輪新增，未進一步處理）。
- F135：噶瑪蘭族臺東縣現況人口數字未能取得，見上方說明，留待下一輪。

## 3.1 追記——PR #24 開出後即收到 Codex bot P1 審查，屬實並已修正

PR #24 開出（非 draft）後數分鐘內，`chatgpt-codex-connector[bot]` 提出一則 P1 意見：本輪 F134 改寫的措辭把「三間村」跟「樟原村」「大峰峰」並列為聚落，但 08-08 F9 已明確查證「三間村」只是長濱鄉六個行政村之一，其下是否另有一個獨立聚落名「三間厝」查證後**尚未證實**，不應與行政村混為一談——這正是本輪自己在改寫時，一邊聲稱「解決 08-06 F1 的實體過度簡化」，一邊又不小心重犯了同一類錯誤（把行政村當聚落）。

核對後確認**屬實**，非誤報：已在 PR 分支追加 commit（`b470659`），改為只列兩個有查證支持的聚落名（樟原部落、大峰峰），並明確加註三間村（行政村）與三間厝（未證實聚落名）的區分，回覆並 resolve 該則 review thread。**這是本輪對自己剛完成的修正的即時反面查核，值得記錄**：即使是「只是執行既有查證結果」的小修正，改寫過程本身仍可能引入新的層級混淆，不能假設「沿用舊查證」就等於「這次的措辭一定正確」。

## 4.1 追記——F136：本輪自己一次示範了「修正過度簡化時，改寫措辭本身又引入新的過度簡化」

與 §4 原本記錄的「無」不衝突（那是指本站對台東的研究性推翻），但這裡補記一個方法論教訓：F134 的改寫意圖是解決 08-06 F1 的「實體過度簡化」錯誤，執行時卻在措辭層面重蹈覆轍——把已查證的行政村（三間村）與尚待證實的聚落名（三間厝）並列陳述。這由外部 review（非本輪自查）抓到，如實記錄，不淡化為「小筆誤」。

## 6. 下一步

- B-004 已結案，`ops/BACKLOG.md` 已同步標記；PR #24 已修正 Codex P1 意見，CI 綠燈，mergeable，等待人類 merge。
- F135 若有意願的下一輪可嘗試下載原民會月度人口統計 Excel、解析臺東縣噶瑪蘭族現況人口，補進 `docs/CLAUDE.md`（非必要，現有修正已消除原本三種錯誤，這只是錦上添花）。
- 16 件待審 PR（本輪未變動總數，`docs/CLAUDE.md` 的 B-004 修正另開為第 17 件）依然零真人互動，最舊 PR #2 已逾 32 天。這個狀況已經連續多輪記錄在案（見 B-031），本輪未發現任何足以改變既有判斷的新證據，故本輪不另外提報，僅如實更新天數。
- BACKLOG 🟠 清單裡 B-008（COVERAGE 矩陣初始化，0% 覆蓋）是 B-007 結案後排序最前的未觸及項目，但依步驟 2 的「預設是產出可上線的內容，不是產出更多方法論」精神，這是一個框架建置工作而非內容產出，下一輪若無新內容可補、也無其他被跳過的具體項目時，才考慮啟動。

---

本輪 `ops/JOURNAL`、`ops/METRICS.md`、`ops/BACKLOG.md` 依步驟 1.5 範圍表直接 commit 到 main。`docs/CLAUDE.md` 修正命中族群名（噶瑪蘭族）與人口描述，依 `ops/BACKLOG.md` B-004 原始分級為 T3，已開 PR（第 17 件待審 PR），不進 main。
