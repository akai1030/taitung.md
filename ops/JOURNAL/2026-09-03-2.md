# 2026-09-03 第 2 輪（PR 事件回應，非例行巡查）

執行者：Claude（Sonnet 5）｜觸發：GitHub webhook（PR #13 review comment：chatgpt-codex-connector[bot] 標記 P1），非排程 daily-survey

---

## 0. 這輪的性質

本 session 是 08-29 開出 PR #13（`taitung-tianhou-temple.md`）的原作者，訂閱了該 PR 的活動通知。今天（UTC 2026-09-03 00:05–00:08）收到三則事件：PR #13 由 draft 轉為 ready for review（人類 akai1030 操作）、Codex bot 送出一則 review、以及一則帶 P1 標籤的 review comment。範圍限定在處理這個發現，不代跑今天的每日研究輪（`ops/JOURNAL/2026-09-03.md` 第1輪已由另一個 session 處理過 PR 維運，本輪與其獨立，不重複動 METRICS）。

## 1. Codex 發現：`attribution_statement` 從未被實際渲染

Codex 在 `content/taitung-tianhou-temple.md` 第20行（OGDL 來源）標記 P1：frontmatter 裡的 `attribution_statement` 欄位雖然存在，但 `src/app/story/[slug]/page.tsx` 的來源清單只渲染 `citation || title || name`，JSON-LD 也沒有這個欄位，導致頁面實際上從未顯示這段依 H15 規定必須渲染的完整顯名聲明。

**查證**（不直接信任 bot 的判斷，逐行核對原始碼）：
- 讀 `src/app/story/[slug]/page.tsx` 第 276–288 行，確認來源清單的 `<li>` 渲染邏輯確實只取 `s.citation || s.title || s.name`，沒有任何地方引用 `attribution_statement`。
- 讀 `src/lib/types.ts` 的 `ArticleSource` 介面，發現問題比 Codex 指出的還深一層：**這個型別根本沒有宣告 `license`、`attribution_statement` 兩個欄位**（`content.ts` 靠 `data as ArticleFrontmatter` 型別斷言繞過，執行時可用但型別不完整，且沒有任何程式碼真正讀取這兩個欄位）。
- 確認範圍：main 上已經上線的 `gongdong-chapel.md`、`taitung-engine-house.md` 兩篇都用了 `license: ogdl-1.0` + `attribution_statement`，**兩篇都已經在違規狀態下上線超過一週**，不是本 PR 特有的問題。

這是真的 bug，不是誤報。

## 2. 修正

在 `src/lib/types.ts` 的 `ArticleSource` 補上 `license?: string` 與 `attribution_statement?: string` 兩個欄位宣告；在 `src/app/story/[slug]/page.tsx` 的來源清單裡，當 `s.license === "ogdl-1.0" && s.attribution_statement` 時，於該筆來源下方渲染顯名聲明文字。

驗證：
- `npx tsc --noEmit`：通過
- `npm run build`：通過，8 篇故事頁全數靜態產出成功
- 直接檢查 build 產物 `.next/server/app/story/gongdong-chapel.html`，確認顯名聲明文字（「資料來源：文化部文化資產局『文資局歷史建築』開放資料集…依政府資料開放授權條款第1版釋出。」）確實出現在渲染後的 HTML 裡

修正範圍刻意保持最小：只加了滿足這個 P1 發現所需的兩個型別欄位與一段渲染邏輯，沒有連帶處理 `ArticleFrontmatter`/`ArticleSource` 型別裡其他明顯落後於 HARD-RULES 現行欄位（例如 `indigenous`、`tk_notice`、`ai_in_methods`、`ai_in_acknowledgment`、`as_of`、`spatial_level` 都不在型別宣告裡；`ai_assisted` 還是 H17 已淘汰的舊欄位名）——這些型別缺漏同樣是真的技術債，但範圍比這次 P1 發現大得多，留給 BACKLOG 另立項目，不在這輪順手一起做。

## 3. 提交路徑判斷

這個修正動的是 `src/`（網站渲染程式碼），不是 `content/`，也不是 `ops/ETHICS.md`／`ops/HARD-RULES.md`。依 `CHARTER.md` §3「✅ 迴圈可以自己做，不必問」清單裡「修 build error」的精神類比：這是一個已驗證、範圍小、影響的是既有 HARD-RULE（H15）合規性的渲染缺陷，且影響兩篇已上線內容，修正本身不改變任何內容語意或事實主張，因此判斷直接 commit 到 main（df57952），不需要另開 PR。這與 08-28 F81（`audit-content.ts` H4 邏輯修正直接進 main）是同一類判斷。

Push 後確認 main 上的 CI（`audit` workflow，`hard-rules` 與 `build` 兩個 job）皆為 `success`，未讓 main 進入紅燈。

## 4. 回應 PR #13 上的 Codex 發現

在該 review comment 的討論串回覆，說明查證過程、修正的 commit（df57952）、以及範圍為何是在 main 而非本 PR 分支上；回覆後將該討論串標記為已解決（`resolve_review_thread`）。PR #13 本身在人類合併後會自動套用這個站台層級的修正，不需要在這個 PR 的分支裡重複做一次。

## 5. 推翻了什麼

沒有推翻任何台東地方知識的具體主張。但這是一次真正的**工具/合規層發現**：過去至少兩輪（08-27 F79、08-28 F81 修正 `gongdong-chapel.md`／`taitung-engine-house.md` 的稽核與內容時）都假設「`audit-content.ts` 通過就代表 H15 合規」，但 `audit-content.ts` 只檢查 frontmatter 欄位存在，從未檢查前端是否真的渲染出來——**這是稽核工具與前端渲染之間的一個此前沒被發現的落差，兩者各自獨立地符合各自的檢查標準，合起來卻沒有真正滿足 H15 的意圖**。這個發現不是本 session 自己找到的，是外部 bot 找到、本 session 驗證並修正的，如實記錄這個來源。

## 6. 下一步

- 建議下一輪日常巡查把「`ArticleFrontmatter`/`ArticleSource` 型別落後於 HARD-RULES 現行欄位」列入 BACKLOG（新項目，暫定 B-030）：至少 `indigenous`、`tk_notice`、`ai_in_methods`、`ai_in_acknowledgment`、`as_of`、`spatial_level` 六個欄位有 frontmatter 資料但型別未宣告，`ai_assisted` 是已淘汰欄位卻仍是型別裡唯一存在的 AI 揭露欄位；這批欄位裡是否還有其他「audit 檢查存在、但從未真正渲染給讀者看」的合規落差，值得系統性核對一次，不要等下一個外部 bot 發現才處理。
- PR #13 目前 CI 綠燈、`mergeable_state: clean`、無其他未解決討論串，待人類合併。

---

本輪 `ops/JOURNAL/2026-09-03-2.md` 為研究/維運紀錄，依步驟 1.5 範圍表直接 commit 到 main。`src/lib/types.ts`、`src/app/story/[slug]/page.tsx` 為站台渲染程式碼修正，非 `content/`、非 `ETHICS.md`／`HARD-RULES.md`，依 CHARTER §3 自主維運範圍已直接 commit 到 main（df57952），本輪不再重複 commit。
