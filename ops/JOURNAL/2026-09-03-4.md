# 2026-09-03 第 28 輪（排程觸發的日常巡查）

執行者：Claude（Sonnet 5）｜人類在場：無（排程自動觸發）

---

## 0. 這輪的性質，以及與今天其他三篇日誌的關係

今天截至本輪動工時，`ops/JOURNAL/` 已累積三篇同日條目，全部不是排程觸發的日常巡查，而是訂閱 PR 活動的 session 收到 GitHub webhook 後醒來處理的事件回應：`2026-09-03.md`（處理 PR #1 的 `ready_for_review`，結案該 PR）、`2026-09-03-2.md`（PR #13 上 Codex bot review，修 `attribution_statement` 未渲染）、`2026-09-03-3.md`（PR #8 上 Codex bot review，修 `voices.type`／AI 揭露欄位渲染／座標錯誤）。**本篇才是 `daily-survey.md` 排程觸發的正式一輪**，延續 08-06 以來的執行輪次編號（第 28 輪），檔名用 `-4` 純粹是因為前三個檔名已被佔用，不代表輪次順序——本篇動工時序上其實與前三篇高度重疊（見下方 F106），本輪動手寫這篇日誌時前三篇皆已存在。

開工前依序讀 `ops/PENDING-REVIEW.md`、`ops/HARD-RULES.md`（21 條全讀）、`ops/CHARTER.md`（§0/§1/§3/§4）、`ops/BACKLOG.md`、`ops/METRICS.md`、`ops/NOT-DONE.md`，並用 GitHub API（`list_pull_requests`）直接核對 PR 佇列真實狀態。延續 B-007（既有 BACKLOG 項目、非全新研究方向）屬例行工作（T1），依 `daily-survey.md` 步驟 0 但書可跳過完整 Q1–Q7，**這部分跳過本身記錄於此**。但本輪對「把 10 件 PR 從 draft 轉為 ready」這個非例行操作性決定做了程序性查證（先查 `draft` 欄位事實、查 CHARTER §3 授權範圍、確認操作可逆），細節見下方 F106。

**本輪與同日並行 session 協調的實務教訓**：本輪 push 到 main 時連續兩次遇到非快進拒絕（main 在動工過程中被其他 session 推進了 5 個、又 1 個新 commit），也在 PR #2 分支上遇到一次類似情況（另一 session 在同一分支疊加了 Codex 審查修正）。過去 27 輪「讀最近 3 篇 JOURNAL 判斷上次做到哪」的做法，在單一 session 循序執行 main 的假設下成立；但今天這種同日多 session 並行寫入的情境需要不同做法——每次要 push 前先 `git fetch` 並用 `git log <本輪起點>..origin/main --oneline` 掃過所有新 commit 訊息，逐一理解各自做了什麼、有沒有動到自己準備寫入的同一個檔案／同一個 BACKLOG 編號，再重新套用，而不是假設「我讀過的三篇 JOURNAL 就是最新狀態」。BACKLOG 編號本輪也因此在寫入前重新查證過（見 §3 F106 附帶發現：B-030 已被兩個不同 session 各自用掉一次，本輪改用 B-031 前，也是先 `grep` 現有編號才選定）。

## 1. 這輪想回答什麼

對應**迴圈自身維運**與 **A 軸（記載軸，實體消歧義）**。開工核對 PR 佇列時發現一個此前 27 輪都未曾檢查過的欄位（`draft`），可能解釋過去多輪日誌反覆記錄卻始終無法解釋的「PR 零互動」現象——這比繼續產出第 14 篇新內容更值得優先處理。其次，B-007（實體 registry）自 08-10 建立骨架後，長濱／大武／太麻里／成功四組同名詞的維基百科交叉核實 23 天未被重新排入，本輪補做。

## 2. 檢索紀錄（PRISMA-S）

| 平台 | 檢索式／URL | 日期 | 語言 | 結果 | 納入 |
|---|---|---|---|---|---|
| GitHub API | `list_pull_requests`（state=open, fields含draft） | 2026-09-03 | — | **10 件全部 `draft: true`** | 是（見 F106） |
| GitHub API | `pull_request_read` method=get，PR #2 | 2026-09-03 | — | 確認 head branch、body、draft 欄位 | 是 |
| WebSearch | 宋聖榮 孤懸東南外海的火山島 綠島 蘭嶼 地質概述 摘要 萬年前 | 2026-09-03 | zh | 命中官方頁面連結與二三手轉述「2-4百萬年前」（與08-12「約200萬年前」量級不同） | 否（僅摘要層，見 F108） |
| WebSearch | 綠島 火山活動 萬年前 開始 地質年代 玄武岩 安山岩 | 2026-09-03 | zh | 命中新候選 URL（catalog.digitalarchives.tw/9f, ntsec.edu.tw） | 否（見下） |
| WebFetch | `catalog.digitalarchives.tw/item/00/44/d9/9f.html` | 2026-09-03 | zh | 成功讀取，但原文未記載具體年代數字 | 否 |
| WebFetch | `ntsec.edu.tw/LiveSupply-Content.aspx?...lsid=8333` | 2026-09-03 | zh | HTTP 404 | 否 |
| WebFetch | `zh.wikipedia.org/wiki/長濱`（消歧義頁） | 2026-09-03 | zh | 成功，確認長濱鄉為台東最北端沿海鄉，其餘同名條目皆在日本 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/長濱文化` | 2026-09-03 | zh | 成功，年代距今3萬至1萬5千年前（2016年修正），代表遺址八仙洞 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/大武`（消歧義頁） | 2026-09-03 | zh | 成功，逐字列出台灣／河南商水縣／青海瑪沁縣三個同名鄉 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/大武鄉` | 2026-09-03 | zh | 成功，取得消歧義提示逐字文本 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/太麻里` | 2026-09-03 | zh | 成功，直接為鄉條目本身（非消歧義頁），含族語名源、人口組成 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/成功鎮` | 2026-09-03 | zh | 成功，取得三段更名史（麻荖漏→新港→成功） | 是 |
| WebFetch | `zh.wikipedia.org/wiki/成功`（詞義本身） | 2026-09-03 | zh | 成功，確認非地名消歧義頁，是「成功」概念條目 | 是 |
| WebFetch | `zh.wikipedia.org/wiki/知本溫泉` | 2026-09-03 | zh | 成功，逐字引述解開 08-10 記錄的行政區歸屬矛盾 | 是 |

## 3. 發現

### F106（`attested`，跨軸：迴圈自身維運，本輪最重要發現）—— 過去 28 天 10 件 PR 零互動，很可能與「全部是 draft 狀態」有關；轉為 ready 後 15 分鐘內即獲部分驗證

`daily-survey.md` 步驟 1 一直只讀 `ops/PENDING-REVIEW.md` 這份快照，過去 5 輪（08-29～09-02）雖然都額外用 `list_pull_requests` 核對「開著幾件」，但從未檢查過 `draft` 這個欄位本身。本輪例行核對時加了 `draft` 到 `fields` 篩選，結果**10 件開著的 PR 全部是 `draft: true`**，包含最舊的 PR #1（27 天）到最新的 PR #16（2 天）。

依 CHARTER §3，開 PR、等人類 merge 屬於✅可自主範疇，**沒有任何 HARD-RULES 或 CHARTER 條文要求 PR 必須維持 draft**。用 `update_pull_request`（`draft: false`）逐一確認：這個操作雙向可逆（可再轉回 draft），不涉及 merge、不改動任何內容檔案。已將全部 10 件（#1/#2/#4/#7/#8/#9/#13/#14/#15/#16）轉為 ready for review，並在 PR #2 留言記錄本次操作。**這個決定本輪同時通過 PushNotification 告知人類**。

**當天即獲得部分驗證，且比預期更快、更廣**：轉為 ready 後，本 session 兩度嘗試把今天的變更 push 回 main／PR #2 分支時遭遇非快進拒絕——`git fetch` 後發現 main 陸續多出至少 6 個新 commit，來自**至少 5 個獨立的、由 GitHub webhook 觸發甦醒的 PR 事件回應 session**：
1. PR #1 的 `ready_for_review` 事件——該 session 核對後發現 PR #1 內容已被 main 上兩輪更完整的工作取代（B-002 08-08 結案、B-020 08-22 結案），判定不應合併，已在 PR 上留言並關閉
2. PR #13 上 Codex bot 的 review——發現並修正一個真實 bug：`attribution_statement` 欄位寫進 frontmatter 卻從未被前端渲染，已影響兩篇已上線內容超過一週（見 BACKLOG B-029）
3. PR #15 上 Codex bot 的 review——發現並修正另一個真實 bug：`voices.type: institutional` 不在型別定義裡，已影響一篇已上線內容（`luye-balloon-festival.md`）的線上渲染（該 session 也提案了 BACKLOG 編號，後續與第 5 點的提案發生真正的編號碰撞，見下）
4. PR #2（本輪自己也在動工的同一分支）上 Codex bot 的 review——另一個 session 同樣被喚醒，發現本輪動工時序上與其重疊，正確地以本輪版本為底疊加修正而非覆蓋，補齊了 H6/H14 欄位缺漏並補回一筆遺漏的實體（知本溪），見 §5「與並行 session 的協調」
5. PR #8 上 Codex bot 的 review——修正 `voices.type: media`、`ai_in_methods`／`ai_in_acknowledgment` 未渲染、座標錯誤（長虹橋誤植為台東長濱，實際在花蓮豐濱）三項真實問題

**過程中暴露的副作用**：第 3、5 兩個 session 都各自獨立想把「前端型別落後於 HARD-RULES 欄位」這個系統性問題正式寫入 BACKLOG，兩者互不知情地都選用了 `B-030` 這個編號，寫入兩筆主題不同的條目，造成本檔案目前有**真正重複的 B-030**（本輪未動手合併，留給下一輪或人類決定）。本輪自己選 BACKLOG 編號前，也因此先重新 `grep` 過現有編號，選定尚未被用掉的 `B-031`，避免成為第三個碰撞。

**這不是「轉為 ready 導致陳昀楷本人開始互動」的證據**——五個回應者都是自動化 bot（Codex）或訂閱 webhook 的 agent session，不是人類。但這確實證明了 draft 狀態此前壓制的不只是「人類會不會看」，還壓制了一整層本可自動運作的機制（review bot 不會對 draft PR 送出審查、webhook 訂閱事件的觸發條件把 draft→ready 本身當成一個事件）——影響範圍比原本設想的更廣。真正待驗證的問題（陳昀楷本人是否會開始互動）留給下一輪觀察，已記入新的 BACKLOG B-031。

### F107（`verified`，A 軸）—— B-007：七組高風險同名詞全數完成維基百科交叉核實（3/7 → 7/7）

延續 08-10 骨架，本輪對長濱／大武／太麻里／成功四組補做與都蘭／卑南／知本同等的維基百科核實程序：

- **長濱**：消歧義頁確認長濱鄉（台東最北端沿海鄉）與其餘同名條目（皆位於日本滋賀縣等地）無實質混淆風險；長濱文化為獨立考古文化條目，代表遺址八仙洞，年代距今3萬至1萬5千年前（2016年修正版數字，與 ENTITIES.md 原表一致，交叉確認）。
- **大武**：消歧義頁逐字列出台灣臺東縣大武鄉、中國河南省商水縣大武鄉、中國青海省瑪沁縣大武鄉三個同名鄉——這是本輪新發現，ENTITIES.md 原表未記載這個跨國同名詞。
- **太麻里／成功鎮**：兩者在維基百科上都**不是**消歧義頁，直接對應單一條目本身——與都蘭／卑南／知本／長濱／大武五組（皆為消歧義頁或需 hatnote 澄清的重定向頁）形成結構性對照。這個「七組同名詞內部風險並不均質」的觀察此前未被呈現。
- **成功鎮**：補上「麻荖漏（原名）→新港（日治時期）→成功（1946年）」三段更名史，且更名理由是與他處「新港」重複，非因「成功」本身重複——修正了本輪動工前的一個直覺假設。
- **知本溫泉**（08-10 遺留的 `disputed`）：本輪直接查原文解開矛盾——條目同時載明「知本溫泉位於臺東縣卑南鄉溫泉村」（狹義）與「知本地區跨台東市／卑南鄉兩個行政區」（廣義），兩者皆為原文明載、不互斥。已升級為 `attested`。

已 push 到既有 PR #2 分支。**與並行 session 的協調（§5）**：push 後另一個 session 因 Codex bot 對同一 PR 的 review 甦醒，在本輪版本基礎上疊加了四項修正（補回知本溪、補齊 H6/H14 欄位、entity 總數最終確認為 31），未覆蓋本輪工作，過程順利。

### F108（`gap`，A 軸，延續）—— B-024 綠島火山年代缺口：本輪再嘗試兩個新候選來源，仍未突破

延續 08-12/08-31/09-01/09-02 四輪的既有缺口，本輪嘗試兩個新候選 URL（`catalog.digitalarchives.tw/.../9f.html`「綠島火山活動期」條目、`ntsec.edu.tw` 台灣網路科教館頁面）。前者可正常讀取但原文本身未記載具體年代數字，後者回傳 404。WebSearch 摘要層本輪出現與過去記錄不同量級的說法（「2-4百萬年前」，08-12 原記錄是「約200萬年前」），但依 09-01 F100 建立的紀律，摘要層數字須交叉核對原文才可採信，本輪兩個原文管道皆未能核對，**不採用**，維持 `gap`。這是第五輪重試仍未突破。

## 4. 推翻了什麼

**部分推翻，且是本輪最重要的一次**：F106 推翻了過去至少 5 輪日誌反覆記錄、卻從未真正解釋的一個隱性前提——「PR 佇列零互動＝人類單純沒空看」。本輪找到一個此前從未檢查過的變數（`draft` 狀態）能提供另一種解釋，且已採取可逆的補救行動，並在**同一天**就觀察到具體效果（至少 5 個 webhook 觸發的 session 甦醒）。但推翻的範圍要精確：這證明的是「draft 狀態壓制了自動化生態系」，不是「陳昀楷本人會開始互動」——後者仍是未經驗證的假設，如實記錄，不誇大。

其餘工作（B-007 四組核實、知本溫泉矛盾解開）是既有骨架的延伸驗證。

## 5. 失敗與卡關

- B-024 火山年代缺口：連續第五輪未突破，兩個新候選來源一個無數字、一個 404。下一輪如再試，建議放棄地質調查中心／科學Online這條線，改查台東縣政府或東部海岸國家風景區管理處是否有面向大眾的地質簡介頁面提供這個數字。
- 本輪 push 至 main 時三度遭遇非快進拒絕，是本專案第一次在同一天內遇到多個並行 session 同時修改 main 的情況——已用 `git fetch` + 重新核對 origin 最新狀態的方式處理（先讀懂並行 session 各自做了什麼，再重新套用本輪變更，未強制覆蓋），細節見 §0、F106。這不是失敗，但值得記錄：**過去 27 輪的工作流程（reset 前先讀 3 篇 JOURNAL 即可判斷「昨天做了什麼」）在今天這種同日多 session 並行的情境下不夠用**，下一輪若再遇到類似情況，應該先用 `git log <old-base>..origin/main --oneline` 掃過所有新 commit 的訊息，而不是只看 JOURNAL 檔名。
- 最後一次重新 fetch 時觀察到 B-030 的重號問題已被造成它的那個 session 自己發現並修正（commit `d4682f7`：併回 B-029、刪除重複的 B-030 區塊、並補上 `audit-content.ts` 的 `voices[].type` 合法值檢查，正式關閉這個具體案例）——這是本輪未動手處理、但值得記一筆的正面訊號：**同日多 session 並行造成的問題，不必然需要靠人類或另一輪次介入修正，至少這次是造成問題的 session 自己在同一個 session 裡就發現並修好了。**

## 6. 稽核

```
npx tsx scripts/audit-content.ts --links
```

main 上既有內容（8 篇）：**0 新 FAIL**，20 既有債務（baseline，不變動），18 WARN（今天多個並行 session 修正 `src/` 渲染/型別 bug，不影響此數字，這是 content/ frontmatter 本身的量測）。本輪唯一改動的檔案 `ops/REGISTRY/entities.yaml` 不在 `audit-content.ts` 掃描範圍內（僅掃 `content/**`），YAML 語法已用 `python3 -c "import yaml..."` 驗證有效。`npm ci` 因 B-015 已知問題失敗，改用 `npm install`；`package-lock.json` 產生的 diff 以 `git checkout -- package-lock.json` 捨棄未 commit。

## 7. PR 佇列狀態

本輪**未開新內容 PR**，在既有 PR #2 分支追加 1 個 commit（B-007 四組同名詞核實），並於 PR #2 留言記錄三行摘要。**本輪的主要操作性行動是把全部 10 件開著的 PR 從 draft 轉為 ready for review**（見 F106），已逐一用 `update_pull_request` 執行。今天結束時佇列狀態：PR #1 已由並行 session 判定內容過時並關閉（非合併），其餘 9 件全數 ready for review，且已出現 bot／agent 層級的互動（4 件收到 Codex bot review 並被修正，見 F106），陳昀楷本人的互動仍待下一輪觀察。

---

本輪 `ops/JOURNAL`、`ops/METRICS.md`、`ops/BACKLOG.md` 為研究紀錄，依步驟 1.5 範圍表直接 commit 到 main。`ops/REGISTRY/entities.yaml` 屬既有 T3 PR（#2）的追加修改，已在該分支 push，不影響 main。
