# 2026-08-20 第 14 輪（日常巡查）

執行者：Claude（Sonnet 5）｜人類在場：無（排程自動觸發）

---

## 0. 框架挑戰（GOVERNANCE §0.5）

例行工作（延續既有 BACKLOG 項目 B-027），依步驟 0 但書可跳過完整 Q1–Q7——**跳過本身記錄於此**。

開工前依序讀 `ops/PENDING-REVIEW.md`、`ops/HARD-RULES.md`、`ops/CHARTER.md`、`ops/BACKLOG.md`、
`ops/METRICS.md`、`ops/NOT-DONE.md`、最近一篇 JOURNAL（2026-08-19）。並用 GitHub API
（`list_pull_requests`）直接核對 `ops/PENDING-REVIEW.md` 是否過期：**7 件 PR 全部仍未合併、
`updated_at` 與 `created_at` 完全相同（代表自開啟以來零評論、零互動）**。PR #1 建立於
2026-08-07，今天是第 **14 天**；#2 第 11 天；#3 第 9 天；#4 第 8 天；#5 第 7 天；#6 第 6 天；
#7 第 5 天。

**今天的關鍵判斷**：`content/` 目錄下仍只有 5 篇文章，全部已在待審 PR 裡補完到零 FAIL，main
上尚未生效。依 `ops/PENDING-REVIEW.md` 的明文警告（2026-08-08 的教訓：不要重做已完成的
工作），今天不重跑這五篇的補完，也不開新的第 8 個內容 PR——B-022（GRB/NDLTD 系統性掃描）
已於昨天結案，今天延續其後繼項目 **B-027**（從 1370 筆候選中逐筆核實內容），對應 A 軸
（記載軸），純 ops 工作，直接 commit 到 main。

## 1. 這輪想回答什麼

對應 **A 軸（記載軸）**。B-027 昨天新增時列出三個優先順序，今天做第一項：

> F56 提到的 GRB `529433`〈花東地區噶瑪蘭人的歷史變遷（1874-1945年）〉——若可核實，
> 直接支援已結案的 B-002。

具體問題：這筆候選是真實存在的研究計畫嗎？摘要或主持人資訊能不能取得？跟已知的噶瑪蘭研究
系譜對得上嗎？

## 2. 檢索紀錄（PRISMA-S）

| 平台 | 檢索式／URL | 日期 | 語言 | 結果 | 納入 |
|---|---|---|---|---|---|
| WebFetch | `grb.gov.tw/search/planDetail?id=529433` | 2026-08-20 | zh | **不可用**——回傳系統升級維護通知頁（「本系統將於 5/30(六) 8:00~20:00 進行系統升級作業」），非計畫資料 | 否（記為缺口） |
| WebSearch | `"花東地區噶瑪蘭人的歷史變遷" 1874-1945 中央研究院` | 2026-08-20 | zh | 9 筆結果，未直接命中 GRB 計畫頁，但命中中研院台史所研究員詹素娟頁面與其博士論文題目 | 是（間接佐證） |
| WebFetch | `ndltd.ncl.edu.tw/handle/54194319273121165471`（詹素娟博士論文紀錄） | 2026-08-20 | zh | **不可用**——圖形驗證碼硬關卡，與 B-001（2026-08-09）已記錄的 NDLTD 存取限制一致 | 否 |
| WebFetch | `twcenter.org.tw/wp-content/uploads/2015/05/g02_10_02_01.pdf`（詹素娟文章全文） | 2026-08-20 | zh | HTTP 503 | 否 |
| WebFetch | `ith.sinica.edu.tw/members_faculty_look.php?...id=71`（詹素娟中研院台史所人員頁） | 2026-08-20 | zh | 可用，全文讀取成功 | 是 |

## 3. 發現

### 🔵 F57（`inferred`，明確標記推論鏈）—— GRB 529433 與詹素娟研究系譜高度重疊，但**未核實**主持人

GRB 候選 529433 的執行機構「中央研究院台灣史研究所籌備處」與主題關鍵詞（噶瑪蘭人、花蓮
地區、台東地區、遷徙），跟該所研究員詹素娟已知的博士論文題目〈族群、歷史與地域──噶瑪蘭
人的歷史變遷（從史前到1900年）〉（1998）及其合編的《臺東縣史：人物篇》（2001）主題高度
重疊。**這是推論，不是核實**——GRB 詳細頁本輪仍不可存取（見下方失敗記錄），沒有任何一筆
來源直接列出 529433 的主持人姓名。完整推論鏈與兩筆獨立來源（不融合）記在
`ops/CORPUS/2026-08-20-grb-529433-verification.yaml`。

**這筆對 B-002／B-020 的實際意義有限，需要誠實說清楚**：即使核實了主持人是詹素娟，
這篇計畫談的是 1874-1945 年的歷史遷徙，回答的是「歷史層記載是否存在」（B-002 已結案的
問題，此為又一佐證），**不回答**「台東噶瑪蘭族當代生活的第一人稱記載」（B-020 仍未跑完
的窄問題）。不能把這筆拿來聲稱 B-020 有進展。

### 🟡 F58（`attested`）—— NDLTD 圖形驗證碼硬關卡今天再次確認存在

`ops/BACKLOG.md` B-001 已於 2026-08-09 記錄 NDLTD 有驗證碼硬關卡，今天嘗試存取具體論文
詳細頁（而非批次匯出 CSV，那條路徑本來就繞過了介面）再次撞到同一堵牆。這不是新發現，是
既有已知限制的再次確認——記錄是因為 daily-survey 要求「查了發現沒問題／沒變」也要誠實寫
（見昨天 F53 的同一原則），不能因為結果不意外就跳過。

## 4. 推翻了什麼

**沒有。** 今天是連續第 2 天沒有推翻任何先前假設（昨天 08-19 也是 0，見該篇 F53 說明）。
今天的工作性質是候選核實與方法論確認，不是需要暴露矛盾的第一手內容查證。**尚未達連續
三次「無」的確認偏誤警戒線**，但已經是第 2 次，下一輪若再次沒有推翻，需要主動檢討是不是
選題本身傾向「確認」而非「挑戰」。

## 5. 失敗與卡關

- **GRB 詳細頁本輪不可存取**：`grbsearch.stpi.narl.org.tw`／`grb.gov.tw` 兩個網域先前
  分別記錄過 503（`ops/BACKLOG.md` B-001）與今天的系統升級維護頁，**同一個系統換了不同
  的不可用理由**，代表 GRB 詳細頁級別的資料（摘要、主持人）目前對本迴圈而言是**結構性
  不可及**，不是單次暫時故障。B-027 的「逐筆核實」如果指的是核實 GRB 詳細頁內容，這條
  路目前走不通；今天改用外部佐證（機構人員頁）部分彌補，但那是**間接推論**，不是核實
  原始計畫本身。
- **NDLTD 論文詳細頁驗證碼**（F58）：同上，結構性不可及，非本輪新發現。
- **環境筆記（非研究失敗，供下一輪參考）**：本沙箱的 `npm ci` 因 lockfile 與 Node 版本
  不同步而失敗（`ops/BACKLOG.md` B-015 已知問題），改用 `npm install` 可跑但會**修改
  `package-lock.json`**——本輪執行 `npm install` 後檢查發現 lockfile 有 diff，**刻意
  `git checkout -- package-lock.json` 捨棄該變更、未 commit**，因為本沙箱是 Node 22，
  專案 pin Node 20，此環境產生的 lockfile 可能在 CI 的 Node 20 上不同步，貿然 commit
  有重演 2026-08-13 F32（main 稽核假紅燈）同一類錯誤的風險。`audit-content.ts` 改用
  `npm install` 產生的 `node_modules`（未 commit 的部分）直接執行，結果與預期一致
  （0 新 FAIL、46 既有債務、34 WARN，見下方稽核結果），代表這個繞過法本身沒有引入問題，
  只是不該把 lockfile 的副作用留下來。

## 6. 稽核

`npx tsx scripts/audit-content.ts`（`npm install` 而非 `npm ci`，見上方失敗記錄）：

```
檢查 5 篇內容 ｜ 0 新 FAIL ｜ 46 既有債務(baseline) ｜ 34 WARN
```

與 2026-08-19 的最後確認狀態一致（37→46 的落差是 baseline 定義口徑差異，非本輪產生，
見 `ops/METRICS.md` 08-13 F32 說明）；34 WARN 含「今日無日誌」一項，本篇寫入後回落至 33，
與過去每日模式相同。**main 上 `content/` 樹本身今天未變動，此為預期中的確認性稽核，
不是新的驗證。**

## 7. ⚠️ 需要人類注意的事（本輪額外重點）

**待審 PR 佇列已經 14 天沒有任何人類互動，這已經不是「今天判斷正確」的問題。**

7 件 PR（#1–#7）自各自建立以來，GitHub API 回報的 `updated_at` 與 `created_at` 完全相同——
沒有評論、沒有 review、沒有關閉。最早的 PR #1（噶瑪蘭族台東記載辯證探究）已經開了 **14 天**。
這批 PR 涵蓋的是：5 篇既有內容補完到零 FAIL（`content/`）＋ 1 個實體 registry 骨架
（`ops/REGISTRY/`）＋ 1 個已結案的辯證探究（`ops/BACKLOG.md` 等）。

**METRICS 的「有完整來源的內容」已經連續 5 天卡在 5/5（研究完成度），但上線內容數字
（真正影響網站的）仍是 0/5**——因為 5 篇全部卡在未合併的 PR。這個落差本迴圈無法自己
解決（`CHARTER.md` §3：PR 要等人類 merge，不能自我合併）。連續多天（08-17 起）刻意不
新增第 8 個內容 PR，是為了不讓佇列更長，但佇列本身已經超出 daily-survey 設計時假設的
「人類大致跟得上」的節奏。**本輪透過 PushNotification 工具主動通知**（前幾輪日誌記錄
「已通知」但 daily-survey.md 目前文字明寫「無通知管道」——這次確認本次執行環境確實提供
了通知工具，前後不一致值得下一輪留意，可能是執行環境本身的差異，不代表流程文件本身錯誤）。

## 8. 下一步

- B-027 繼續核實候選清單其餘項目，尤其 NDLTD 中的部落層級題目（金峰鄉 sapulju 部落、
  都歷部落 Pacefongan 正名等），優先度高於再挖 GRB 529433（該筆已撞牆，見上）。
- **PR 佇列問題已連續多輪標記，若下一輪（08-21）佇列仍是 7 件且仍零互動，建議下一輪
  也主動通知，不要因為「已經講過」就降級為只寫日誌**——沉默的重複不等於問題解決。
- GRB 系統升級公告寫的是「5/30」，若那是固定週期性維護窗口，日後排程執行如果剛好落在
  該時段會重複撞到，值得記一筆但不急著現在處理。

---

本輪 `ops/JOURNAL`、`ops/METRICS.md`、`ops/CORPUS/2026-08-20-grb-529433-verification.yaml`
皆為研究基礎設施與方法論紀錄。CORPUS 條目雖標記 `indigenous: true`（涉及噶瑪蘭族），但依
步驟 1.5 範圍表，`ops/CORPUS/` 不在 H10（僅限 `content/` 與 `ops/REGISTRY/`）範圍內，是
研究紀錄而非網站內容，故直接 commit 到 main，非開 PR。本輪未變更 `content/`，未開新 PR，
`package-lock.json` 的環境副作用已捨棄未 commit。
