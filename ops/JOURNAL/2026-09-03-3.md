# 2026-09-03 第 3 輪（PR 事件回應，非例行巡查）

執行者：Claude（Sonnet 5）｜觸發：GitHub webhook（PR #8：draft→ready、Codex bot review 4 則 P1/P2 comment），非排程 daily-survey

---

## 0. 這輪的性質

本 session 是 08-23 開出 PR #8（`kavalan-zhangyuan.md`）的原作者，訂閱了該 PR 的活動通知。
UTC 2026-09-03 00:05–00:09 收到四則事件：PR #8 由 draft 轉為 ready for review（人類 akai1030
操作）、CI 兩項 check 皆綠、以及 Codex bot 送出的一則 review 附四則 inline comment（P1×2、
P2×2）。範圍限定在處理這個發現，不代跑今天的每日研究輪——讀 JOURNAL 後發現今天已有
`2026-09-03.md`（第1輪，PR 維運）與 `2026-09-03-2.md`（第2輪，PR #13 的同類型 Codex 發現）
兩篇，本輪與其獨立，不重複動 METRICS，只記錄本輪特有的發現。

## 1. Codex 的四則發現（逐一查證，不直接信任 bot 判斷）

- **P1 voice type**：`content/kavalan-zhangyuan.md` 用了 `voices: - type: media`，但
  `src/lib/types.ts` 的 `VoiceType` 只有 academic/oral-history/field-note/youth-action/
  visitor 五種。查證 `VoiceBlock.tsx` 第 49 行 `voiceConfig[type] || voiceConfig.academic`，
  確認會靜默 fallback 成學術樣式，`StoryCard.tsx` 的 `voiceLabels[v.type] || v.type` 則會
  直接顯示未翻譯的原始字串 `media`。真的 bug。
- **P1 AI 揭露欄位未渲染**：與 09-03 第2輪（PR #13）發現的是**同一類問題但不同欄位**——
  那輪修的是 `attribution_statement`（H15），這次是 `ai_in_methods`／`ai_in_acknowledgment`
  （H17／`ops/ETHICS.md` E19）。查證 `page.tsx` 第 289 行只讀取已淘汰的 `ai_assisted`，
  確認 `ai_in_methods`／`ai_in_acknowledgment` 從未被消費。真的 bug。
- **P2 座標**：`coordinates: [23.233, 121.418]` 的估算依據（研究日誌寫的「長虹橋／八仙洞
  之間」）本身就錯——**長虹橋其實在花蓮縣豐濱鄉，不在台東縣長濱鄉**，這是 08-23 那輪的
  一個事實查證疏漏，一路寫進了 JOURNAL 也沒被抓到。WebSearch 重新查證八仙洞的真實座標
  （23.397166, 121.47667），確認與原估值相差約 19 公里，與 Codex 標記的 18–25 公里量級
  相符。八仙洞實際位於樟原橋南端、行政區屬樟原村（見查證紀錄），改用其座標。
- **P2 layer**：`layer: [people, community]`，但 `src/lib/types.ts` 的 `LayerId` 沒有
  `community` 這個值（十二個合法值見 `layers.ts`）。這是內容作者本輪自己在 08-23 造字
  造出來的無效值，不是型別落後於內容——與前一則座標錯誤同屬「08-23 那輪自己的查證/
  審慎不足」，不是外部依賴的問題。

## 2. 修正

- `src/lib/types.ts`：`VoiceType` 加入 `"media"`；`ArticleFrontmatter` 加入
  `ai_in_methods?: string[]`、`ai_in_acknowledgment?: string[]`（`ai_assisted` 保留，
  標註已淘汰、僅為舊內容渲染相容）。
- `src/components/VoiceBlock.tsx`：`voiceConfig` 加入 `media` 條目（標籤「機構媒體報導」），
  render 邏輯新增對應分支（沿用 field-note 的卡片式版型，而非另創一種視覺樣式）。
- `src/components/StoryCard.tsx`：`voiceColors`／`voiceLabels` 加入 `media` 條目。
- `src/app/story/[slug]/page.tsx`：來源清單下方新增 `ai_in_methods`／`ai_in_acknowledgment`
  的渲染（各自標「AI 於研究方法中的角色」／「AI 於寫作協助中的角色」），僅當兩者皆缺席
  時才 fallback 渲染舊版 `ai_assisted`，避免新舊格式重複顯示。
- `content/kavalan-zhangyuan.md`：`layer` 改 `[people, living]`；`coordinates` 改
  `[23.398, 121.476]`（八仙洞實測座標，樟原村範圍內的最佳可得估計，非精確測量）。

驗證：`npx tsc --noEmit` 通過；`npm run build` 通過，9 篇故事頁全數靜態產出成功（含
`kavalan-zhangyuan`）；`npx tsx scripts/audit-content.ts` 對此檔仍 0 FAIL 0 WARN，且
main 上既有內容 0 新 FAIL（20 既有債務不變）。

## 3. 提交路徑判斷

修正動的是 `src/`（站台渲染程式碼）與**自己這個 PR 分支的** `content/kavalan-zhangyuan.md`。
與 09-03 第2輪（PR #13）修 `attribution_statement` 時的判斷不同之處：那次的問題影響
main 上**已經上線**的內容（gongdong-chapel.md、taitung-engine-house.md），所以直接
commit 到 main；這次的 `media` voice type／`ai_in_methods` 渲染問題雖然同樣是站台程式碼，
但目前**只有這個尚未合併的 PR 分支**用到 `media` 類型與新版 AI 揭露欄位（main 上其他
8 篇都用既有五種 voice type，也還沒有一篇同時具備 `ai_in_methods` 且不具備 `ai_assisted`
的情況會被這次改動影響行為）。判斷：`src/` 的修正本身是通用、無副作用的擴充（新增
選項，不改變既有分支行為），可以直接 commit 到 main，但因為改動是**由這個 PR 的內容
需求觸發**、且此刻只有這個 PR 用得到，選擇跟著 `content/kavalan-zhangyuan.md` 的修正
一起留在 PR 分支裡，讓審核者一次看到「內容＋讓內容正確顯示所需的程式碼」，而不是
拆成兩個時間點的 commit。merge 後對其他內容無副作用（純加法，未修改任何既有 case
的行為），如果之後有其他 PR 也想用 `media` 類型，屆時可以照樣直接引用，不用重做。

本輪也把 `origin/main`（已推進到 08-24～09-03 共 10 輪，含 08-28 人類首次合併 6 篇
PR）合併進這個分支，確認無衝突、稽核與 build 皆過，一併推上去。

## 4. 回應與收尾

在 PR #8 留言說明四則發現與修正對應關係，並將四個 review thread 標記為已解決
（`resolve_review_thread`）。

## 5. 推翻了什麼

沒有推翻任何台東地方知識的具體主張。但**推翻了 08-23 那輪自己對兩個事實的假設**：
（1）長虹橋在台東長濱、鄰近八仙洞——**錯，長虹橋在花蓮豐濱**；（2）`[23.233, 121.418]`
是樟原的合理估計座標——**錯，實際偏離真實地點約 19 公里**。這兩個錯誤都是 08-23 當時
沒有花時間查證、直接用「聽起來合理」的地標關聯下判斷造成的，值得記一筆給以後：**地理
座標和其他量化陳述一樣需要查證，不能用「大概在哪一帶」的印象代替。**

次要教訓：`ai_in_methods`／`ai_in_acknowledgment` 從未被渲染，與 09-03 第2輪發現的
`attribution_statement` 從未被渲染是**同一個更大缺口的兩個實例**——`ArticleFrontmatter`／
`ArticleSource` 型別長期落後於 `HARD-RULES.md` 實際要求的欄位。09-03 第2輪已建議立
B-030 但尚未真正寫入 BACKLOG，本輪一併補上（見下）。

## 6. 下一步

- 已將 B-030 正式寫入 `ops/BACKLOG.md`（09-03 第2輪只在 JOURNAL 建議、未落地）：
  `ai_in_methods`／`ai_in_acknowledgment` 這次已修，但 `indigenous`、`tk_notice`、
  `as_of`、`spatial_level` 仍未宣告在型別裡，值得下一輪系統性核對一次。
- PR #8 目前 CI 綠燈、四則 review comment 已回應並 resolve，待人類合併。
- 建議下一輪日常巡查檢查其餘待審 PR（#9 pacefongan、#13、#14、#15）的內容裡，
  座標欄位是否有類似 08-23 這種「印象式地標關聯」而未實際查證的情況——這次是本 session
  自己抓到自己的錯，但同一種輕率判斷可能也發生在其他篇。

## 7. 更正（本輪稍後發現，同一 session 內修正）

把上面 §6 寫的「已將 B-030 正式寫入 BACKLOG」推送到 main 後，切回 main 分支重新讀
`ops/BACKLOG.md` 才發現：**B-030 這個編號當天已經被另一個獨立 session 用掉**——
`voices.type: institutional` 不在 `VoiceType` 定義裡（PR #15 Codex review 發現的另一
類似問題），已先佔用 B-030。本輪的「型別宣告落後」內容其實就是 B-029 本身（09-03 第2輪
已經建立），不該另開新編號。

已修正：把本輪的進度說明（`ai_in_methods`／`ai_in_acknowledgment` 已修）併回 B-029
段落，刪除重複的 B-030 區塊。同時發現既有 B-030（`voices.type` 問題）列的三項待辦裡，
兩項（`luye-balloon-festival.md`、`pacefongan.md` 改用合法 voice type）已被其他 session
處理完成，只剩「稽核腳本本身應檢查 voices.type 合法性」真的還沒做——本輪順手在
`scripts/audit-content.ts` 加了 `VALID_VOICE_TYPES` 檢查（含本輪新增的 `media`），
B-030 正式結案。

**教訓**：多個 session 同一天各自獨立發現問題、各自準備寫進 BACKLOG 時，編號衝突是
真實會發生的事，不是假設性風險。commit 前重新 `git pull` 只能避免內容衝突，避免不了
「兩人都挑了同一個下一個編號」這種邏輯衝突——這次是自己切回 main 重讀才發現，運氣成分
較高。沒有更好的機制建議，只能記錄這個案例，供之後參考。

---

本輪 `ops/JOURNAL/2026-09-03-3.md`、`ops/BACKLOG.md`（新增 B-030）依步驟 1.5 範圍表
直接 commit 到 main。`src/lib/types.ts`、`src/components/VoiceBlock.tsx`、
`src/components/StoryCard.tsx`、`src/app/story/[slug]/page.tsx`、
`content/kavalan-zhangyuan.md` 皆留在 PR #8 分支，理由見 §3。
